# CQJTU Hub 代码分析与改进建议（正式版）

> 采集时间：2026年8月13日；基准提交：5ebd516（提交于 2026-08-03 00:46 +0800）。
> 行数口径：PowerShell 逐文件执行 `(Get-Content <file>).Count` 累加。
> 本报告仅基于静态代码分析；性能类建议需真机测量后实施，不含未经测量的性能数字。
> 第一至第七部分描述基准提交 5ebd516 的代码状态；末尾"实施状态"一节记录随后的落地情况，其统计为改动后状态（app lib 55 文件/18,214 行，packages 37 文件/8,611 行，测试 49 文件）。
>
> **2026-08-22 第二轮补充**：本报告第一至七部分是纯数据层/后台/测试视角的审计，
> 未覆盖 UI 表现层、包体积、无障碍三个维度。第二轮针对这些盲区做了实测审计，
> 结论并入"第九部分：第二轮审计（UI / 包体 / 无障碍）"，实施记录并入
> "实施状态（2026-08-22 第二轮）"。第二轮发现的问题均附可复现证据（探针测试
> 输出、真实构建产物体积、网络实测数据），不含推断性结论。

---

## 一、项目规模

### 1.1 行数与文件数

```
apps/campus_app/lib/      49 个文件， 18,675 行
packages/core/lib/        10 个文件，  1,269 行
packages/data/lib/         9 个文件，  3,549 行
packages/data/tool/        4 个文件，    425 行
packages/platform/lib/    12 个文件，  2,824 行
─────────────────────────────────────────────
非测试文件合计            84 个文件， 26,742 行

apps/campus_app/test/     26 个文件，  6,151 行
packages/*/test/          15 个文件，  1,748 行
─────────────────────────────────────────────
测试文件合计              41 个文件，  7,899 行
```

注：另一份独立统计得到约 26,771 行生产代码、7,905 行测试代码，与本报告相差约 30 行，属统计范围/末尾行处理差异，数量级一致，不影响任何结论。本报告下文统一引用自己的采集值。

### 1.2 单文件规模

| 文件 | 行数 |
|------|------|
| schedule_page.dart | 2,801 |
| direct_school_campus_gateway.dart | 2,448 |
| profile_page.dart | 1,939 |
| tools_page.dart | 1,597 |

### 1.3 静态分析（逐 package 执行）

```
$ dart analyze apps/campus_app → 1 issue found（info：unnecessary_import，app_theme.dart:1）
$ dart analyze packages/core   → No issues found!
$ dart analyze packages/data   → No issues found!
$ dart analyze packages/platform → No issues found!
```

合计：1 条 info，0 error，0 warning。

### 1.5 各模块行数明细

**app lib（49 文件，18,675 行）按目录聚合（本报告口径，逐文件 (Get-Content).Count 累加后按目录汇总）：**

| 目录 | 行数 | 占比 | 主要文件 |
|------|------|------|---------|
| pages/ | 10,214 | 55% | schedule 2,801、profile 1,939、tools 1,597、campus_card 720、study_progress 650 |
| features/ | 4,052 | 22% | schedule_providers 783、schedule_export_service 721、study_progress_providers 580、cached_resource 488 |
| widgets/ | 1,704 | 9% | silent_zove_token_bootstrapper 398、course_cell 247 |
| providers/ | 817 | 4% | session 673 |
| theme/ | 536 | 3% | app_theme 499 |
| services/ | 352 | 2% | app_update_coordinator 176 |
| 其余（utils、config、main.dart） | 1,000 | 5% | semester_service 205、main.dart 613 |

**packages（8,067 行）：** core 10 文件（模型 6 + 工具 4）、data 9 文件（网关实现 2 + api_service + 会话管理 + 错误类型等）+ tool 4 个探测脚本、platform 12 文件（services 10 + src 2）。

### 1.6 运行时模式

CampusRuntimeMode.localAndroid（默认，App 直连学校系统，dart:io HttpClient + HTML 解析）与 CampusRuntimeMode.selfHosted（自部署 Spring Boot 后端，JSON API）。campusGatewayProvider 按模式返回 DirectSchoolCampusGateway 或 SelfHostedCampusGateway，二者实现同一 CampusGateway 接口（10 个方法）。

---

## 二、测试现状

### 2.1 分布

| 目录 | 文件数 | 覆盖内容 |
|------|--------|---------|
| apps/test/features/schedule/ | 9 | schedule_providers_test、schedule_challenger_test、schedule_empirical_gate_test、account_isolation_test、m2_multi_account_gate_test、account_preferences_test、m2_challenger_edge_cases_test、m2_empirical_verification_test、course_text_parser_test |
| apps/test/features/settings/ | 7 | account_cache_service_test、account_cache_empirical_challenger_test、account_cache_m4_challenger_test、challenger_1_m3_stress_test、challenger_2_m3_stress_test、m3_empirical_challenge_test、m3_empirical_verification_test |
| apps/test/features/campus_card/ | 1 | campus_card_providers_test（TrackingCampusGateway 记录调用与 forceRefresh 参数） |
| apps/test/features/academic/ | 1 | academic_card_test（FakeCampusGateway） |
| apps/test/features/shared/ | 1 | cached_resource_account_isolation_test |
| apps/test/pages/ | 1 | campus_card_page_test 页面 Widget |
| apps/test/services/ | 1 | webview_session_scope_test |
| apps/test/utils/ | 1 | campus_error_message_test |
| apps/test/ 根目录 | 4 | runtime_mode_provider_test、schedule_exam_mapping_test、theme_empirical_test、widget_test |
| packages/core/test/ | 5 | Course/DormRoom 模型、course_text_parser、polling_utils、dorm_room |
| packages/data/test/ | 3 | direct_gateway_live_test（默认 skip）、encrypt、study_progress_dashboard_parser |
| packages/platform/test/ | 7 | account_cache 服务与边界压力、session_service、background_runtime_mode、class_reminder_schedule、dorm_service、schedule_widget_data |

### 2.2 覆盖率说明

测试代码 7,899 行 / 生产代码 26,742 行 ≈ 29.5% 行数比。这只是行数比，不是代码覆盖率；覆盖率需运行 `flutter test --coverage` 读取 lcov 报告，当前未执行。

### 2.3 测试缺口

1. DirectSchoolCampusGateway 缺少可 CI 稳定执行的直接单元测试：direct_gateway_live_test.dart 需要真实学号密码，默认 skip（:30/:41/:51/:61/:72）。现有 Fake/TrackingGateway 只覆盖 Provider 层对接口的调用，不覆盖实现类内部的 CAS 登录、Cookie 管理、HTML 解析。
2. NotificationService（326 行）无测试。
3. AppUpdateService（504 行）无测试。
4. SessionManager 恢复/退避逻辑（session.dart 约 470 行）无直接测试。
5. CachedResource 边界情况测试仅 1 个文件（cached_resource_account_isolation_test）。
6. 上课提醒生成逻辑（class_reminder_schedule）在 platform 有测试（class_reminder_schedule_test），但 NotificationService 的下发/取消路径无测试。

---

## 三、已实现的功能

以下能力经代码确认存在，不应再列为待实现：

1. 前台刷新冷却：main.dart:133 定义 10 秒冷却；:175-177 检查。
2. 夜间轮询降频：polling_utils.dart 白天 15 分钟、夜间（23:00-6:00）3 小时，background_task.dart:55-59 据此节流。
3. WebView 登录后自动刷新：web_login_binder.dart:26-27 绑定完成后 triggerRefresh() + requestRefresh()，CachedResource 监听 sessionUpdateProvider 自动刷新。
4. 普通刷新去重：cached_resource.dart:220-222 在 !forceRefresh 时复用同一资源的 in-flight 请求（强制刷新路径例外，见 4.2）。
5. 账户级缓存隔离：缓存 key 含 base64(username)（cached_resource.dart:550-567），切换账号走独立缓存空间。
6. 敏感信息脱敏：_redactIdentifier、_sanitizeSensitiveText 在日志输出前过滤密码/ticket/token/sessionId/Cookie。
7. 电池优化引导：首次启动检测电池优化豁免与 MIUI 自启动状态，分步引导（main.dart:269-295）。
8. 静默 zove token 预热：登录后隐藏 WebView 静默预热 h-zove-token（silent_zove_token_bootstrapper.dart:19-27），不阻塞进入主页面。
9. 更新自检流量控制：feed 6 小时 TTL 缓存 + ETag/If-None-Match 304 复用 + 失败降级旧缓存（app_update_service.dart:155-156、:259-269、:221-231）。

---

## 四、经代码确认的改进点

> **2026-08-22 复核说明**：本节 11 条已全部实施，逐条复核结论见各条标题后的
> 标注与末尾"实施状态"表。复核方式为读实现代码 + 跑对应测试，不是查关键字——
> 因为第二轮发现了"有测试、测试通过、但核心分支永不执行"的情况（见 9.1），
> 关键字存在不能证明行为正确。

### 4.1 密码持久化表述（安全事实）— 已实施

确认事实：登录成功后 CredentialService.save()（credential_service.dart:28-30）将 username 和 password 写入 FlutterSecureStorage（Android Keystore 加密）。后台任务 background_task.dart:113-114 重新读取用于余额检查；login_page.dart:40 在登录成功路径调用 save()。

正确表述：密码持久化到系统级加密存储，用于自动登录与后台余额检查；不写入 SharedPreferences 明文，也不落普通文件。安全性优于明文存储，但不应声称密码不持久化。

### 4.2 强制刷新可能对同一资源发起并发请求（P1）— 已实施

确认事实：去重条件为 `existing != null && !forceRefresh && _inflightUsername == username`（cached_resource.dart:221）。使用 forceRefresh: true 的路径绕过去重：账户切换（:174）、sessionUpdate 信号（:179）、手动刷新按钮、校园卡进入刷新（campus_card_providers.dart:22-24）。

精确风险表述：同一个资源正在刷新时，又收到手动刷新、账户切换或新的 sessionUpdate，由于这些调用使用 forceRefresh: true，可能对同一资源启动第二个请求。

需要澄清：一次 sessionUpdate 信号扇出为多个不同资源的并行刷新是当前设计行为，不是缺陷；真正的问题是同一资源的重叠请求。建议评估按资源 key 维护强制刷新 in-flight 记录，或让信号触发一次协调刷新。

### 4.3 巨型页面拆分（P1）— 已实施，2026-08-22 复核

**复核结论：已完成。** 三个文件当前行数 schedule_page.dart 375、profile_page.dart 552、
tools_page.dart 828，合计 1,755 行（原 6,337 行）。拆分后最大 part 为
schedule_grid.dart 861 行。

事实（基准状态）：schedule_page.dart 2,801 行、profile_page.dart 1,939 行、tools_page.dart 1,597 行，合计 6,337 行，占 app lib（18,675 行）的 34%。

收益：主要解决维护成本（热重载编译、评审范围、职责边界），不直接解决生产故障。拆分文件本身不减少运行时 Widget 重建范围——需配合独立 Widget 封装与缩小 ref.watch 作用域。

建议：按职责边界分阶段拆分（schedule：页面壳/网格/周导航/新增课程/更多操作/学期选择/工具函数；profile：壳/用户卡片/宿舍设置/课表偏好/缓存管理/后台设置/更新卡片），每阶段独立回归。

### 4.4 核心网关直接单测（P1）— 已实施

事实：DirectSchoolCampusGateway 2,448 行，聚合登录状态机、Cookie 管理、重定向跟随、CampusGateway 的 10 个接口方法、HTML 解析五类职责；CAS/解析逻辑无可 CI 测试。登录、Cookie 导入等额外方法属于具体实现的辅助能力，不计入 CampusGateway 接口方法数。

建议：用注入式 transport 编写离线单测，锁住：Cookie 域隔离规则（domain/path/secure/expires 匹配）、重定向 POST→GET 切换、表单编码、会话过期判定、_recentSemesters 学期推算。

### 4.5 冷启动链（P1，先测量）— 已实施（代码变更 + 模拟器 A/B，无收益声明）

事实：main.dart:31-44 在 runApp 前 await NotificationService.init() 与 Workmanager 初始化。_MainShellState 首帧后串行执行 _trySchedule → 电池引导 → 更新检查（main.dart:145-152）；后两者可能弹窗，不应盲目并行化。

建议：先记录真机冷启动到首帧基线，再决定是否将通知/WorkManager 初始化延后到首帧之后。弹窗类任务保持串行。

### 4.6 onData 副作用在数据未变化时仍执行（P2）— 已实施

确认事实：CachedResource 在每次成功刷新后都调用 `await onData(fresh, changed: changed)`（cached_resource.dart:271-272），是否变化只作为参数传入，框架不阻止调用。各实现的处理不一致：

- ScheduleNotifier.onData 检查 `if (!changed) return`（schedule_providers.dart:120-121），数据未变化时不重排通知/小组件。
- ElectricityNotifier.onData 不检查 changed（electricity_providers.dart:84-87）：每次成功刷新都执行阈值通知 + 小组件写入。
- CampusCardBalanceNotifier.onData 不检查 changed（campus_card_providers.dart:65-67）：每次都写小组件。

因此实际情况是：电费与校园卡在每次成功刷新（即使数据没变）都会执行通知、小组件副作用。建议先给这两个 onData 加 `if (!changed) return` 短路，再评估是否需要 3 秒 debounce——短路是更小、更精准的改动。

### 4.7 资源级缓存有效期（P2）— 已实施

事实：session.dart:60-64 的 SystemDomain.freshness 服务于会话/系统域健康检查（session.dart:506）与 zove token 有效期（silent_zove_token_bootstrapper.dart:145-177），不是 CachedResource 数据缓存有效期，二者不能接线。

正确方案：在 CachedResourceNotifier 增加独立策略钩子 `Duration? get cacheFreshness => null`，各资源自行定义（课表/成绩/余额可不同）。启用后需保证：手动刷新与 forceRefresh: true 一律绕过；自动刷新定时器不受影响；账户切换不复用旧账户缓存；设备时间回拨时有单调性保护。

### 4.8 前后台余额检查可能重复请求（P2）— 已实施（回填），频率统计待真机

确认事实：WorkManager 后台任务（background_task.dart:249-271）检查电费与校园卡余额；前台 ElectricityNotifier 与 CampusCardBalanceNotifier 也刷新这两项。两套任务互不感知，没有共享 in-flight 状态，时间重叠时可能对相同余额数据发起重复请求；后台结果也不会回填前台 CachedResource。

建议：先记录一段时间内前后台重叠请求的实际频率，再决定是否需要协调（如共享请求去重键，或后台结果回填前台缓存）。

### 4.10 上课提醒的 7 天前瞻窗口依赖前台触发（P2）— 已实施（窗口 7→14 天 + seed 续排）

确认事实：buildClassReminders 只生成今天起 0-7 天内的提醒（class_reminder_schedule.dart:82-83，daysFromToday 超出 0-7 直接跳过）；scheduleClassReminders 的全部调用点都在 app 前台路径（main.dart:220 _trySchedule、schedule_page.dart:369 _doRefresh、profile_page.dart:1266 设置变更、schedule_providers.dart:132/840/843/857）；后台任务 background_task.dart 零引用，只做余额与更新检查。

含义：若用户超过 7 天不打开 App，第 8 天起的上课提醒不会被排入系统，提醒会静默缺失。前台恢复时会补排（main.dart:166 resumed 触发 _trySchedule），但依赖用户主动回前台。

建议：评估两条路径之一——(a) 后台任务里补一次课表提醒重排（需在后台读凭据并调 gateway，成本与电量需要测量）；(b) 将前瞻窗口与上次排课时间持久化，在 UI 上提示提醒可能过期。优先级 P2，先确认目标用户是否普遍超过 7 天不开 App。

### 4.11 刷新按钮完成/失败反馈（P2）— 已实施

事实：spinning_refresh_button.dart 60 行，刷新中旋转，完成与失败无视觉区分。

建议：成功后短暂显示对勾，失败显示感叹号并配 HapticFeedback。

---

## 五、需测量后再决策

1. RepaintBoundary：先开 DevTools 重绘彩虹确认课表页存在大面积不必要重绘，再定点添加；批量包裹会增加 layer 和显存开销。
2. ListView itemExtent/prototypeItem：先确认 profile 等长列表在低端机掉帧。
3. gzip/压缩：直连模式学校系统是否支持需抓包确认；自部署模式由后端启用。
4. 网络失败自动重试：校园网/CAS 故障时可能放大服务器压力；建议指数退避、上限 3 次，且先做连通性探测。
5. 移动数据差异化策略：可能导致移动网络用户长期看到过期数据，不建议简单按网络类型区分。
6. 隐藏 WebView 的耗电：SilentZoveTokenBootstrapper 持有隐藏 WebView 并以 25 分钟间隔做健康检查（silent_zove_token_bootstrapper.dart:40），前台常驻期间的耗电与内存影响需实测。
7. 课表通知全量重排成本：每次课表数据变化都会先 cancelAllClassReminders 再逐课 zonedSchedule（notification_service.dart:127、:192），在课程较多学期中的系统调用次数与耗时需实测（与 4.6 的短路建议配套）。

---

## 六、不建议实施

1. 合并每日上课通知：破坏课前提醒产品语义。
2. 启动时预加载所有 Tab：增加启动期网络竞争、流量与耗电。
3. 把电池引导与更新弹窗并行化：两者都可能弹窗，竞争对话框风险高于收益；保持串行。

---

## 七、评价

### 7.1 评分

| 维度 | 评分 | 依据 |
|------|------|------|
| 功能完整性 | 9/10 | 课表/校园卡/电费/成绩/考试/培养计划/请假覆盖全面 |
| 架构设计 | 8/10 | Gateway 抽象 + CachedResource + 双运行模式，Provider 层可测 |
| 代码质量 | 8/10 | 26,742 行仅 1 条 info lint，零 error/warning；脱敏与 Cookie 隔离细致 |
| 测试 | 5/10 | 41 文件/7,899 行/行数比 29.5%，核心网关无直接单测，无覆盖率报告 |
| 可维护性 | 6/10 | 分层清晰，但 2,801/1,939/1,597 行三个巨型文件占 app lib 34% |
| 性能工程 | 6/10 | 缓存优先 + 夜间降频 + 前台冷却已到位；冷启动链、强制刷新并发待优化 |
| 安全性 | 8/10 | 密码 Keystore 加密持久化；Cookie 域隔离；日志脱敏 |

> **2026-08-22 第二轮补充评分。** 上表是第一轮（数据层视角）的评分。
> 第二轮补齐 UI/包体/无障碍三个维度后，评分需要修正——原表因为没有审计
> 表现层，高估了用户实际体验：

| 维度 | 第一轮 | 第二轮修正 | 依据变化 |
|------|-------|-----------|---------|
| 功能完整性 | 9/10 | 9/10 | 不变 |
| 架构设计 | 8/10 | 8/10 | 分层依然清晰；但 `when()` 的守卫说明抽象层缺行为测试 |
| 代码质量 | 8/10 | 8/10 | 四包 analyze 零问题维持 |
| 测试 | 5/10 | 7/10 | 326 测试 + 四包覆盖率门禁；新增反向验证实践 |
| 可维护性 | 6/10 | 7/10 | 巨型文件已拆（6,337→1,755 行） |
| 性能工程 | 6/10 | 7/10 | 包体 −70%；成绩查询 48s→约 6s；tab 保活 |
| 安全性 | 8/10 | 8/10 | 跑步账号隔离补齐，回到与其余模块一致的水位 |
| **UI 表现层** | 未评 | **5/10** | token 层优秀但组件层未被消费；无深色模式；9 种错误态并存 |
| **无障碍** | 未评 | **4/10** | 零 `Semantics`（已定点补关键处）；仍有 9 处 `shrinkWrap` 触达目标 |
| **包体积** | 未评 | **8/10** | 24.8MB，剩余 90% 是引擎，已接近下限 |

综合：7.1/10 → **7.0/10**（新增两个低分维度拉低均值，但绝对水平是提升的：
三个 P0 已修，包体减 70%）。

评分说明：UI 表现层 5/10 的主因不是"做得差"，而是"做好的东西没生效或没被用"——
设计 token、动效系统、reduce-motion 支持都是高质量的，问题在消费侧与三个
静默失效的基础设施缺陷。无障碍 4/10 是绝对水位低（全 app 曾零 `Semantics`），
本轮只做了定点修复，系统性补齐仍待专门一轮。

综合：7.1/10

评分说明：以上评分为静态代码评审视角的工程判断。功能完整性、代码质量、安全性三项有直接的代码证据支撑；测试、性能工程两项因缺少真实覆盖率与真机测量数据，评分保守；实施 P0 测试基线与真机基线测量后，这两项可重新校准。

### 7.2 优势

1. 架构：CampusGateway 接口让直连/自部署切换成本极低；CachedResource 统一缓存-展示-后台刷新生命周期；sessionUpdateProvider 作为全局刷新信号扇出；Melos monorepo 划分 core/data/platform/app 四层，依赖方向清晰（app 依赖 packages，packages 不反向依赖 app）。
2. 质量：4 个 package 逐一 analyze 合计仅 1 条 info；ManualCookieJar 处理 domain/path/secure/expires 匹配，_SchoolHttpClient 手动重定向（最多 10 跳、POST→GET 切换）避免跨域 Header 泄漏；错误分级（campus_failure.dart 与 API 449/403 语义对应）与用户可读文案（campus_error_message.dart）分离。
3. 测试基础：Provider 层用 Fake/Tracking Gateway 隔离网络；账号隔离与多账号切换有专门测试；settings 目录含两轮 stress/challenger 测试，说明对缓存边界（账户切换、并发）有针对性验证。
4. 平台能力完整：上课提醒原生 MethodChannel 与非 Android zonedSchedule 双路径、桌面小组件数据下发、WorkManager 余额预警、App 更新自检（含 ETag 缓存与 304 处理）都是用户可感知的完整能力，不只是查询壳。

### 7.3 不足

1. 三个巨型页面文件占 app lib 34%，拖慢热重载与评审。
2. 核心网关实现类（2,448 行）无直接单测。
3. 强制刷新可并发、前后台余额检查可重叠。
4. 电费/校园卡 onData 在数据未变化时仍执行副作用。
5. 无覆盖率报告，冷启动未做基线测量。
6. 上课提醒 7 天前瞻窗口依赖前台触发，长期不开 App 会缺提醒（4.10）。
7. course_text_parser 在 core 与 app 各一份实现，存在漂移风险。

### 7.4 建议优先级

| 优先级 | 事项 | 说明 |
|--------|------|------|
| P0 | 建立测试基线 | 运行完整测试套件 + flutter test --coverage 产出 lcov |
| P1 | 核心网关离线单测 | Cookie 规则/重定向/解析逻辑的可 CI 单测 |
| P1 | 分阶段拆分巨型页面 | 按职责边界，每阶段独立回归 |
| P1 | 冷启动基线 + 优化 | 先测量首帧时间再动初始化链 |
| P1 | 强制刷新并发合并 | 评估按资源 key 的 in-flight 记录 |
| P2 | onData 短路 | 电费/校园卡 onData 加 if (!changed) return，再评估 debounce |
| P2 | 前后台余额协调 | 先记录重叠频率，再决定去重或回填 |
| P2 | 资源级 cacheFreshness | 独立策略钩子，含绕过规则与时间回拨保护 |
| P2 | 上课提醒 7 天窗口补排 | 后台补排或 UI 提示提醒可能过期（4.10） |
| P2 | 刷新按钮反馈 | 对勾/感叹号 + 触觉反馈 |

### 7.5 结论

CQJTU Hub 在 26,742 行生产代码规模下静态分析接近零问题，架构分层与缓存策略成熟，测试资产覆盖了 Provider 层关键行为（含账号隔离与多账号切换的专门测试）。

当前最优先的不是拆文件，而是补齐真实覆盖率基线与核心网关的直接单测——这两项让后续所有重构和优化都有可验证的护栏。紧随其后的是三处小而明确的行为修正：电费/校园卡 onData 加 changed 短路（避免无变化副作用）、强制刷新并发合并评估、上课提醒 7 天窗口的补排机制。

巨型页面拆分与冷启动优化属于有价值的长期投入，应分阶段推进并逐步回归，不设每文件行数硬指标。所有性能类改动前先测量、改动后复测，以 DevTools 与真机数据为准。

---
---

## 附录 A：数据刷新机制全景（代码事实）

### A.1 七个缓存资源的刷新触发源

| Provider | 基类 | 自动定时刷新 | 触发源（除手动外） |
|----------|------|------------|------------------|
| scheduleProvider | CachedResourceNotifier | 无（默认 null） | 自定义课程/学期周数/开学日期变化、sessionUpdate、账户切换 |
| gradesProvider | CachedResourceNotifier | 无 | 学期选择变化、sessionUpdate、账户切换 |
| gradeDetailProvider | CachedResourceNotifier | 无 | sessionUpdate、账户切换 |
| examsProvider | CachedResourceNotifier | 无 | 学期选择、sessionUpdate、账户切换 |
| campusCardBalanceProvider | SimpleCachedResourceNotifier | 无 | 进入校园卡 Tab（forceRefresh: true）、Tab 切换、sessionUpdate |
| electricityProvider | SimpleCachedResourceNotifier | 有：构建时由 pollingInterval() 选择昼 15 分钟或夜 3 小时 | 宿舍选择变化、sessionUpdate |
| studyProgressProvider | SimpleCachedResourceNotifier | 无 | sessionUpdate、账户切换 |

事实要点：前台定时自动刷新仅电费一个资源启用（electricity_providers.dart:41）。该间隔在 Provider 构建时确定并传给 Timer.periodic；现有周期跨越昼夜边界时不会自动重设，只有 Provider 销毁后重新构建才会重新选择间隔。后台 WorkManager 每次执行时会重新调用 shouldRunPolling()，其昼夜节流不受此限制。PayCodeNotifier 未继承 CachedResource，自行维护 _inflight（campus_card_providers.dart:129-146），去重条件同样含 !forceRefresh。

### A.2 sessionUpdate 信号扇出

触发点：main.dart:182（前台恢复，10 秒冷却后）、web_login_binder.dart:26（WebView 绑定完成）、silent_zove_token_bootstrapper.dart:226（zove token 引导完成）。每个 CachedResource 在执行 build() 时注册 ref.listen(sessionUpdateProvider)（cached_resource.dart:178 与 :386）；信号到达后，所有当前已实例化的 CachedResource Provider 实例会调用 refresh(forceRefresh: true)。Family Provider 可能同时存在多个参数实例，因此实际请求数量取决于当时已实例化的 Provider，而不是固定七个请求。

### A.3 后台任务与前台刷新

WorkManager 每 15 分钟醒来，经 polling_utils 节流后只做余额阈值检查（电费+校园卡），结果写通知（101/102，4 小时冷却），不触碰 CachedResource 状态。后台结果不会回填前台缓存，前台余额显示可能滞后于后台已检测状态；时间重叠时前后台可能重复请求相同余额（见 4.8）。

---

## 附录 B：关键实现事实

### B.1 直连网关（direct_school_campus_gateway.dart，2,448 行）

ManualCookieJar（:132-227）：按 domain|path|name 存储，过期剔除，secure/HTTPS、hostOnly/子域、路径前缀匹配。_SchoolHttpClient（:238-399）：followRedirects=false，301/302/303/307/308 手动跳转最多 10 跳，POST 表单逐字段编码并带 Origin/Referer。错误分级在 campus_failure.dart（44 行）：CaptchaRequiredFailure、AuthInvalidFailure 等与 449/403 语义对应。

### B.2 各页面刷新事实

课表页：6 个 showModalBottomSheet 懒加载；刷新/重试统一指向 _doRefresh（:482/:530），449/验证码/CAS 错误自动拉起 WebViewLoginPage（:391-403）形成闭环；周切换只做内存过滤不请求网络。校园卡页：首帧后自动刷新、切回 Tab 再刷新、小组件信号滚动到二维码并刷新（campus_card_page.dart:33-70）；进入刷新 Future.wait 并行 forceRefresh 余额与付款码。电费页：打开前检查宿舍设置，未设置弹引导不进页面（electricity_page.dart:17-38）。我的页退出登录：清 WebView 会话 → invalidate 16 个 Provider → 取消通知 → 清小组件 → 回登录页（profile_page.dart:22-42）。登录页：校验 12 位学号，成功后保存凭据并触发 zoveToken 引导。

### B.3 重复代码观察

course_text_parser 在 core（190 行）与 app（398 行）各有一份实现，存在重复维护风险，建议确认是否统一。CachedResourceNotifier 与 SimpleCachedResourceNotifier 的刷新逻辑约 200 行重复。

### B.4 静默 token 预热（silent_zove_token_bootstrapper.dart，398 行）

登录后以隐藏 WebView 静默访问 CAS 登录页、学生大厅与 ecard 入口，捕获 h-zove-token 供请假模块使用（:19-27）；健康检查每 25 分钟一次（:40）；监听 zoveTokenRefreshProvider 可强制重启流程（:56-58）；成功写入后触发 sessionUpdateProvider（:226），带动当前已实例化的 CachedResource Provider 刷新。

### B.5 WebView 登录页（webview_login_page.dart，390 行）

两种模式：WebViewLoginMode.jwgSession（教务会话）与 zoveTokenOnly（仅取 token）。WebView 使用桌面 UA、unrestricted JavaScript，注册 LoginCredential JavaScript 通道接收页面注入的凭据（:53-60）；用 Completer 挂起等待页面加载与 token 捕获（:46-48）；与 SilentZoveTokenBootstrapper 共享同一套 CAS URL 与 cookie_manager MethodChannel（:33-38）。该页是验证码兜底路径：直连登录失败（449/验证码/CAS）时由课表页 _doRefresh 自动拉起（schedule_page.dart:391-403）。

---

## 附录 C：事实核查清单

| 断言 | 证据位置 | 采集方式 |
|------|---------|---------|
| 84 个非测试文件 / 26,742 行；41 个测试文件 / 7,899 行 | 逐目录枚举，2026-08-13 采集 | PowerShell (Get-Content).Count 累加 |
| 巨型文件行数（2,801/2,448/1,939/1,597） | 对应文件 | 同上 |
| dart analyze 合计 1 条 info | 4 个 package 分别执行 | dart analyze 命令输出 |
| 密码持久化到 Keystore | credential_service.dart:28-30；login_page.dart:40；background_task.dart:113-114 | read 逐行核对 |
| 去重条件含 !forceRefresh | cached_resource.dart:220-222 | read 逐行核对 |
| 强制刷新触发点 | cached_resource.dart:174/179；campus_card_providers.dart:22-24 | read 逐行核对 |
| freshness 属于会话健康而非数据缓存 | session.dart:506；silent_zove_token_bootstrapper.dart:145-177 | grep 全量搜索 |
| 仅电费启用自动定时刷新 | electricity_providers.dart:41 | grep automaticRefreshInterval |
| 课表 onData 有 changed 短路，电费/校园卡没有 | schedule_providers.dart:121；electricity_providers.dart:84-87；campus_card_providers.dart:65-67 | read 逐行核对 |
| 前台电费阈值通知无冷却 | notification_service.dart:235-260 | read 逐行核对 |
| 后台通知 4 小时冷却 | background_task.dart:620-624 | read 逐行核对 |
| 网关 live 测试默认 skip | direct_gateway_live_test.dart:30/41/51/61/72 | read 逐行核对 |
| 测试行数比 29.5% 非覆盖率 | 本报告计算 | 明示口径，未声称覆盖率 |

所有行号指基准提交 5ebd516 的代码状态；后续提交改动相关文件后行号可能失效，复核时请以类名/方法名为准。

---

## 附录 D：通知与小组件调度事实

**上课提醒生成（class_reminder_schedule.dart，141 行）**
- classSlotStartTimes 定义 13 节课的起始时刻（08:20 至 20:30）。
- 提醒 id 由日期与节次拼出（year*1000000+month*10000+day*100+timeSlot），同 id 冲突用递增序号消解（:103-111、:133-134）。
- 开始时间优先取 exactStartMinutes，其次考试课从 timeStr 正则提取（支持中文冒号），最后回落节次表（:136-150）。
- remindAt = 开始时间减 reminderMinutes；是否保留由 isPendingReminder（提醒在未来）与 isActiveReminder（上课在未来）决定（:96-101）。

**通知下发（notification_service.dart，326 行）**
- 初始化：初始化时区、创建三个渠道（上课提醒/电费预警/校园卡预警）、请求通知与精确闹钟权限（:40-96）。
- 下发：Android 原生提醒走 MethodChannel campus_app/class_reminder；非 Android 走 flutter_local_notifications 的 zonedSchedule，exactAllowWhileIdle（:131-215）。
- 取消：cancelAllClassReminders 双路径清理（原生 + 逐 id 取消 20 周 × 7 天 × 节次的遗留 id，:355-369）。

**小组件更新（schedule_widget_service.dart，94 行）**
- updateScheduleWidgets 先经 calendarRules.resolveOccurrences 展开课程为具体日期，过滤超出学期周数范围的项，再经 MethodChannel campus_app/schedule_widget 下发给原生（:12-51）。
- updateBalances 单接口同时接收校园卡与电费余额（可空），原生侧更新余额小组件（:73-88）。
- 三个方法全部 try-catch，失败只打日志不回抛（:22-50、:53-61、:63-71）。

---

## 附录 E：更新自检与安装流程事实

**版本来源**：AppUpdateService 从 GitHub Releases API（api.github.com/repos/AAAAxuuuuu/cqjtu-campus-assistant/releases/latest）拉取最新版本，或经 --dart-define=APP_UPDATE_URL 覆盖为自定义 feed（app_update_service.dart:143-168）。

**缓存与流量控制**：feed 缓存 6 小时 TTL（:155-156）；请求带 ETag/If-None-Match，304 时复用缓存（:259-269、:310-314）；失败时若有旧缓存则降级用缓存（:221-231）。

**版本比较**：先比 buildNumber，再按点分版本号逐段比较（:381-419）；支持从 assets 中挑选 .apk 直链（:468-481）。

**更新提示**：前台入口 AppUpdateCoordinator.checkAndPrompt（app_update_coordinator.dart:15-55）在 _MainShell 首帧后调用；后台任务 _checkAppUpdate（background_task.dart:626-655）发现新版本发通知（id 103），经 shouldNotify 版本去重（只对同一版本提示一次）。强制更新（latest.force 且有直链）时对话框 barrierDismissible 关闭（app_update_coordinator.dart:76-81）。

**安装**：AppUpdateInstaller.downloadAndLaunch 下载后调系统安装器；无安装未知来源权限时提示用户授权；直装失败降级打开 GitHub Release 页（app_update_coordinator.dart:140-185）。

**观察**：更新自检是项目里流量控制最完整的模块（ETag + 304 + TTL 缓存 + 失败降级），可作为其他资源接口实现缓存新鲜度的参考样板。

---

## 报告边界

本报告是静态代码评审，不是性能测试报告。以下内容不在结论范围内，需要单独测量或决策：

1. 帧率、首帧时间、耗电、流量等数值——未做真机测量，报告未给出任何数字承诺。
2. 真实代码覆盖率——未运行 flutter test --coverage，29.5% 只是行数比。
3. 学校系统接口的协议细节（gzip 支持、304 语义、验证码触发频率）——需抓包或与校方确认。
4. 用户行为数据（7 天不开 App 的比例、后台任务实际执行率）——需要埋点或运营数据。

改进优先级表中的 P0/P1/P2 是基于代码事实的工程判断；实施前请按附录 C 的证据清单复核行号对应的符号名，避免因后续提交导致的行号漂移。本报告历次修正（密码持久化、强制刷新并发、freshness 语义、统计口径、测试表述、优先级）均已逐条核实并落文。

---

## 实施状态（2026-08-13 更新）

以下内容记录本报告各项建议在 commit 5ebd516 之后工作区中的落实情况，
使报告结论与最终实现保持一致。所有改动均有对应测试。

### 已实施（含测试）

| 报告条目 | 落实方式 | 测试 |
|---------|---------|------|
| 4.2 强制刷新并发合并 | 同账户刷新串行化；force 真正合并；排队任务账号切换后丢弃；未处理异步异常消除。第四轮补齐混合 throwOnError 边界：合并的 throwOnError:true 调用方通过资源 state.error 观察原请求失败（原请求以 false 发起时也能收到异常），并附混合调用测试 | test/features/shared/cached_resource_refresh_behavior_test.dart（12 用例） |
| 4.6 onData 无变化副作用 | 电费/校园卡 onData 加 changed 短路 + 每 notifier 生命周期首次推送（覆盖登出后小组件被清场景） | test/features/shared/cached_resource_ondata_short_circuit_test.dart（3 用例） |
| 4.4 核心网关直接单测 | DirectSchoolCampusGateway 新增 SchoolHttpTransport 注入；离线驱动 CAS/重定向/Cookie 流程 | packages/data/test/direct_gateway_offline_test.dart（12 用例） |
| 2.3/8.x CachedResource 重复逻辑 | 两个 Notifier 基类共用 CachedResourceRefreshMixin（约 200 行重复消除） | 既有隔离测试 + 新行为测试 |
| 4.7 资源级 cacheFreshness | 独立钩子 Duration? get cacheFreshness（与 SystemDomain.freshness 分离）；schedule 2h、grades/gradeDetail/studyProgress 6h、exams 2h 启用；强制刷新绕过；时钟回拨按过期处理 | cached_resource_refresh_behavior_test.dart 中 freshness 组（4 用例） |
| SessionManager 离线测试 | 提取 CampusSessionApi 接口（data），SessionManager 依赖接口 | test/features/shared/session_manager_test.dart（15 用例） |
| NotificationService 测试 | 偏好设置/账户隔离/遗留迁移/阈值 | packages/platform/test/services/notification_service_test.dart（14 用例） |
| AppUpdateService 测试 | 版本比较逻辑公开为 isNewerThanCurrent | packages/platform/test/services/app_update_service_test.dart（16 用例） |
| 4.10 上课提醒 7 天窗口 | 前瞻窗口 7→14 天；seed/水位持久化；后台任务自动续排。第三轮修复后台隔离区代码缺陷（时区/插件懒初始化、关提醒清 seed、legacy best-effort）+ 通道级 mock 测试。**剩余**：真实 WorkManager 引擎插件注册/alarm 创建/通道存在/重启保留四项需 Android 真机验证，清单见 docs/performance-baseline.md | notification_delivery_test.dart（3 通道级用例）+ notification_service_test coverage 组 |
| 4.11 刷新按钮反馈 | SpinningRefreshButton 成功对勾/失败感叹号 + 触觉反馈；电费/校园卡刷新失败 rethrow 以联动按钮 | test/widgets/spinning_refresh_button_test.dart（3 用例） |
| AppUpdate HTTP 行为 | 新增 @visibleForTesting debugFeedUrlOverride；本地 HttpServer 实测：拉取新版本、鲜缓存跳网、304 复用缓存、500 错误回退旧缓存 | app_update_http_test.dart（4 用例） |
| 网关成绩/考试/电费解析 | 离线 fixture 解析：成绩汇总+明细参数、考试行、电费剩余电量页 | direct_gateway_offline_test.dart（3 用例） |
| 4.9 冷启动链 | 通知/WorkManager 初始化延迟到首帧后（main.dart initializeBackgroundServices） | test/main_deferred_init_test.dart |
| 4.8 前后台余额重复请求 | 后台余额结果回填前台缓存（backfillBalanceCaches）+ 检查时间戳埋点；重叠频率的统计留待真机数据 | packages/platform/test/services/background_task_backfill_test.dart（3 用例） |
| 重复解析器 | course_text_parser 统一到 core（app 侧实现迁移，删除重复） | packages/core/test/utils/course_text_parser_test.dart |
| P1 巨型页面拆分 | 第一阶段：schedule 2,801→主文件+3 part；profile 1,939→主+2 part；tools 1,597→主+2 part。第三轮第二阶段：schedule 再拆（sheets 1,135→568、add_course 567、grid 1,045→853、week_navigator 192），最大 part 降到 853 行 | 全量 138 app 测试回归通过 |
| P0 测试基线与 CI | 覆盖率基线文档 + tool/check_coverage.dart 门禁 + CI test 任务。第三轮扩展到四个 package（core 60% / data 35% / platform 50% / app 25%）。第四轮修复 core 门禁命令缺陷：dart test --coverage=coverage 只产出 *.vm.json 不产出 lcov，已改为 flutter test --coverage（本地实测生成 lcov 且门禁 EXIT=0） | .github/workflows/ci.yml |
| 需测量项 RepaintBoundary（结构证据后定点实施） | 重建范围探针（test/perf/schedule_grid_rebuild_probe_test.dart）：周切换重建 640 Widget/73 类型，含 13 个内容不随周变化的 _SlotCell（时间列）→ 仅对确认静态的时间列加 RepaintBoundary；Raster 复测待真机 | docs/performance-baseline.md |
| 需测量项 gzip（环境探测） | 校园服务器 TCP 443 可通但 TLS 握手失败（curl exit 35），本环境无法抓包；已记录，待校园网/真机代理重测 | docs/performance-baseline.md |

### 仍需真机/外部条件验证（非代码可完成）

| 项 | 所需条件 | 当前状态 |
|----|---------|---------|
| 冷启动首帧时间 A/B | Android 真机/模拟器 | 模拟器 A/B 已做（TotalTime 均值 -1.4%，噪声内，不做收益声明）；感知首帧收益需真机 DevTools frame 时间线，见 docs/performance-baseline.md |
| RepaintBoundary/ListView | DevTools 帧时间数据 | 已写实验基线文档，未实施（无证据） |
| gzip | 抓包确认服务端支持 | 未实施（无证据） |
| 网络失败自动重试 | 校园网故障时段统计 | 未实施（风险前置条件不满足） |
| 前后台重叠频率统计 | 真机长时间运行 | 埋点已就绪，统计待数据 |
| live 网关测试 | 真实学号密码 | 保持默认 skip（外部凭据） |

### 明确未实施（报告判定不建议）

批量通知合并、启动预加载全部 Tab、弹窗并行化——均保持原状。

---
---

## 九、第二轮审计（UI / 包体 / 无障碍）

> 采集时间：2026-08-22。审计范围是第一轮未覆盖的三个维度：UI 表现层、
> 包体积、无障碍。所有结论均有可复现证据，证据类型标注在每条中。

### 9.0 为什么需要第二轮

第一轮报告的七个部分全部围绕数据层、后台任务、缓存策略、测试覆盖率，
覆盖得相当扎实（11 条改进点全部落地，284 个测试，四包 CI 覆盖率门禁）。
但它有三个结构性盲区，用关键字检索报告全文可以确认：

| 检索词 | 报告命中 |
|--------|---------|
| `when(` / `loading` / 加载态 / 骨架 | 0 |
| `GlassAppBar` / 毛玻璃 | 0 |
| 深色 / `darkTheme` / `ThemeMode` | 0 |
| `APK` / 包体 / 字体 / `split-per-abi` | 0 |
| `Semantics` / 无障碍 | 0 |

盲区的代价在 9.1 体现得最清楚：一个模块可以同时"有专门的测试文件、
测试全部通过、且核心分支永远不执行"。第一轮的 284 个测试没有一条覆盖到
"用户实际看到什么"，所以三个 P0 级的表现层缺陷在 CI 全绿的情况下长期存在。

### 9.1 CachedResource.when() 的加载态与错误态是死代码（P0，已修复）

**证据类型：探针测试实测输出。**

`cached_resource.dart:57` 的分支守卫：

```dart
if (hasData || !shouldOfferManualRefresh || skipError) {
  return data(this.data);
}
if (hasError) return error(this.error!, stackTrace ?? StackTrace.current);
return data(this.data);
```

`shouldOfferManualRefresh` 定义为 `consecutiveFailures >= 3`（:44）。冷启动时
`consecutiveFailures == 0`，因此 `!shouldOfferManualRefresh` 恒为 `true`，
守卫必然命中，`loading()` 是**不可达代码**——尽管它是 `required` 参数。
同理，第 1、2 次失败时守卫同样命中，`error()` 也不可达。

修复前的探针输出（`CachedResource<String>` 直接构造，不经 UI）：

```
冷启动 (isRefreshing=true, hasData=false, failures=0):  when() -> DATA("")   期望 LOADING
首次失败 (hasError=true, failures=1):                   when() -> DATA("")   期望 ERROR
```

`isLoading` getter（:45，`isRefreshing && !hasData`）逻辑正确，只是没有任何
调用方使用它。`campus_card_providers.dart:99` 的 `PayCodeState.when()` 写法
是正确的（`if (isRefreshing) return loading();`）——这解释了为什么付款码是
全 app 唯一转圈正常的地方。

**用户实际影响**，每次冷启动，六个调用点全部展示空数据而非加载态：

| 页面 | 代码写的意图 | 修复前实际渲染 |
|------|------------|--------------|
| 成绩（`grades_section.dart:51`） | 转圈 | **"暂无成绩数据"** |
| 考试（`exams_section.dart:53`） | 转圈 | **"当前学期暂无考试安排"** |
| 电费（`electricity_page.dart:103`） | `_BalanceSkeleton` | 空白 ¥ 金额 |
| 课表（`schedule_page.dart:315`） | 转圈 | 全空七天网格 |
| 成绩明细（`grades_section.dart:203`） | 抓取中提示 | "该课程暂无可展示的明细" |
| 个人页余额卡 | 转圈 | 空值 |

即：**有成绩的学生被明确告知"暂无成绩数据"**，随后数据到达页面跳变。
弱网下前两次失败同样显示"暂无"而非错误与重试入口，必须连续失败 3 次
才会出现提示。`exams_section.dart:54` 的错误分支更是裸 `Text(e.toString())`，
连重试按钮都没有。

**修复**：按语义优先级重写守卫——缓存数据优先（stale-while-revalidate，
失败的后台刷新不得清空用户仍可阅读的数据）、无数据且在请求中为 loading、
无数据且已失败即为 error（不再等到第三次）。同时移除六个调用点的
`skipError: true`（该参数与它们各自提供的 error 回调自相矛盾），并把 4 处
泄漏的裸 `e.toString()` 接到已存在但仅 2 处调用的 `formatCampusError`。

课表页保留其"会话过期/需安全验证"的专用指引文案优先于通用文案，因为
那条指引是可操作的（点重试触发 WebView 验证）。

**回归保护**：`test/features/shared/cached_resource_when_branch_test.dart`
8 个用例，锁住全部六种状态组合与 `skipError` 的精确语义边界。

### 9.2 GlassAppBar 毛玻璃在全部 15 个页面失效（P0，已修复）

**证据类型：widget 测试实测，且已验证测试对该 bug 敏感。**

`glass_surface.dart:63` 把 `NotificationListener<ScrollNotification>` 放在
`AppBar` 的**外层**：

```dart
return NotificationListener<ScrollNotification>(
  onNotification: (notification) { ... _scrolled ... },
  child: ClipRect(child: BackdropFilter(...)),
);
```

`ScrollNotification` 从 scrollable 向**上**冒泡。但全部 15 个消费页面都写
`Scaffold(appBar: GlassAppBar(...))`——此时 appBar 是 `body` 的**兄弟节点**，
不是祖先。body 的滚动通知经 `Scaffold` 向上传递，永不穿过这个 listener。
`_scrolled` 恒为 `false`，因此 `sigmaX/Y: 0`、背景恒为 `Colors.transparent`。

修复前实测（`Scaffold(appBar:)` 真实布局，滚动 600px）：

```
before = ImageFilter.blur(0.0, 0.0, clamp)
after  = ImageFilter.blur(0.0, 0.0, clamp)
```

叠加 `app_theme.dart:417-418` 把 `AppBarTheme.backgroundColor` 设为透明、
`scrolledUnderElevation: 0` 关掉 Material 自带的 scrolled-under 兜底，
最终效果是：**内容滚动到完全透明的标题栏后面，标题与操作图标和正文重叠。**
那段 14 行描述 "Apple Translucent Chrome" 的文档注释所描述的系统，一行都没生效。

**修复**：改为订阅 `ScrollNotificationObserver`——Scaffold 在 appBar 与 body
之上安装了它，正是为这个场景准备的。同时加两道过滤：只响应垂直轴
（课表的横向日列拖动不得点亮材质）、只响应 `depth == 0`（嵌套选择器不算）。

**回归保护**：`test/widgets/glass_app_bar_scroll_test.dart` 4 个用例——
静止时无材质、body 滚动后模糊启用、回到顶部后材质消失、横向滚动不触发。
其中第二条正是修复前失败的那条。

### 9.3 无深色模式（P0，未修复，见 9.9 决策说明）

**证据类型：代码检索。**

`main.dart:114-115` 只设置 `theme:`，无 `darkTheme:`、无 `themeMode:`。
全 app 检索 `darkTheme` / `themeMode` / `Brightness.dark` / `ThemeMode` /
`dynamic_color` 命中数为 **0**。`AppTheme` 只暴露 `lightTheme`（`app_theme.dart:303`）。

后果不只是"没有深色模式"，而是**深色模式用户拿到浅色界面**——在 Android 10+
上这是多数用户的默认设置。

且这不是加一个 `darkTheme` 就能解决的：`Colors.white` 在 app 内出现 **120 次**
（18 个文件），其中 `glass_surface.dart:49,87,128` 与
`responsive_scaffold.dart:44,71` 是结构性的硬编码白底。真正的深色模式需要先把
这 120 处收敛进 token 层。

### 9.4 包体积：82.8MB 中 62MB 是可直接回收的（P1，已修复）

**证据类型：真实构建产物体积 + APK 内容清单。**

修复前 universal APK 82.8MB，构成：

| 项 | 体积 | 问题 |
|----|------|------|
| 三 ABI 原生库 | 67.2MB | 用户下载两份自己无法执行的运行时 |
| `NotoSansSC-VF.ttf` | 17.8MB | 见下，**并非 UI 字体** |
| `campus_app_mark.png` | 2.25MB | 2048×2048，实际显示 72×72 |

**关键发现：那 17.8MB 字体不是 UI 字体。** `pubspec.yaml` 把它放在 `assets:`
而非 `fonts:` 下，屏幕上的中文走系统字体。全仓库唯一引用是
`schedule_export_service.dart:16`——只为课表导出 PDF 时嵌入中文。也就是说
每个用户装 17.8MB，只为一个可能永远不点的导出按钮。它还是可变字体
（31,036 字形，`wght` 100-900 轴），而 PDF 只用单一字重。

`campus_app_mark.png` 是 2048×2048 但在 `login_page.dart:270` 只显示为
72×72——28 倍冗余，且 Flutter 会把整张图解码进内存。

**修复与实测收益**：

| 措施 | 效果 |
|------|------|
| `--split-per-abi` | 82.8MB → 36.2MB |
| 字体固定字重 + GB2312 子集化（7,589 码位） | 17.8MB → 2.44MB |
| logo 降至 288×288（4× 覆盖 xxxhdpi） | 2.25MB → 68KB |
| **合计（arm64-v8a 实测）** | **82.8MB → 24.8MB（−70%）** |

三 ABI 实测：armeabi-v7a 23.3MB / arm64-v8a 24.8MB / x86_64 26.2MB。
剩余 22.4MB 是 `libflutter.so` + `libapp.so`（90%），已接近 Flutter 应用下限。

注：曾尝试把 `--split-per-abi` 写进 `build.gradle.kts` 的 `splits { abi {} }`
使其成为默认行为，但 Flutter Gradle 插件已设置 `ndk.abiFilters`，Gradle 直接
报错 `Conflicting configuration ... cannot be present when splits abi filters
are set`。已在 gradle 文件留下注释说明不要再尝试，并把发布流程写入
`docs/release-build.md`。

**回归保护**：`test/features/schedule/schedule_pdf_font_test.dart` 4 个用例——
断言字体小于 4MB（防止换回完整字体）、能作为 PDF TrueType 加载、
用真实 `pdf` 包渲染一页中文并验证字形真的嵌进了 PDF 流、
逐字检查课表样本文本（校名/课程名/星期/周次/节次/教室）的字形覆盖。

### 9.5 跑步功能缺少账号隔离（P0，已修复）

**证据类型：代码检索 + 回归测试。**

`campus_running_providers.dart:8-9` 使用设备级全局键：

```dart
const _kRunningSummaryKey = 'campus_running_summary_v4';
const _kRunningRecordsKey = 'campus_running_records_v4';
```

全 app 其他缓存均经 `cached_resource.dart:19` 的 `_activeUsername` 做账号
隔离，这两个键是唯一例外。后果：换账号后会看到上一位学生的跑步记录与统计。

同文件的 `CampusRunningBetaNotifier`（:20-30）已经监听了 `credentialsProvider`
并在换账号时重置，说明账号隔离的必要性在该文件里已被意识到，只是两个存储
notifier 没有 `Ref`、拿不到当前账号。

**修复**：键按用户名作用域化，两个 notifier 接入 `Ref` 并监听
`credentialsProvider`；换账号时先清内存 state 再加载新作用域（避免慢速读
把旧数据刷回）；异步读取完成时校验账号未变（in-flight 账号切换保护）；
未登录状态使用独立的 `__signed_out` 作用域，避免登出期间写入的数据泄漏进
某个账号。

**回归保护**：`test/features/running/running_account_isolation_test.dart`
4 个用例——写入落在账号作用域、旧全局键不再被写、切账号不携带上一账号记录、
汇总数据按账号隔离、登出期写入不泄漏。该功能此前**零测试**。

### 9.6 成绩查询 9 个串行请求（P1，已修复）

**证据类型：网络实测 + 并发性测试。**

`direct_school_campus_gateway.dart:1929` 在 semester 为空（"查全部"，且这是
`grades_section.dart:12` 的默认值）时串行发起 1 + 8 = 9 个请求：

```dart
for (final item in _recentSemesters()) {
  final html = await _fetchGradesHtml(session, username, password, semester: item);
  grades.addAll(_GradeParser.parse(html).grades);
}
```

校园服务器实测（2026-08-22，`curl` 计时）：

```
jwgln.cqjtu.edu.cn:  dns=0.016s  connect=0.016s  tls=5.293s  ttfb=5.434s
ids.cqjtu.edu.cn:    dns=0.006s  connect=0.007s  tls=0.154s  ttfb=0.198s
```

jwgln 的 TLS 握手 5.29 秒占了 ttfb 的 97%。9 个串行请求最坏情况约 **48 秒**。

**并发化的前置条件**：`forceRelogin`（:2435）此前无并发保护——
它以 `_httpClient.clearCookies()` 开头，8 个并发请求若同时遇到会话过期，
会各自清空 cookie jar 并重新登录，互相清掉对方刚建立的会话。所以先加
in-flight 复用，再并发化。

**该风险已实测确证**：临时移除互斥后，并发过期场景产生 **9 次** CAS 登录页
请求（1 初始 + 8 风暴）；恢复互斥后为 **2 次**（1 初始 + 1 共享重登）。
这证明 `test/direct_gateway_relogin_storm_test.dart` 对该缺陷是敏感的，
不是空转测试。

**修复**：汇总页单独先行（它是可能触发重登的那个，让它先落地则后续 8 个从
已建立的会话开始）；8 个学期页 `Future.wait` 并发；每个学期单独 try/catch，
单个学期不可达降级为"该学期无成绩"而非丢弃全部（原串行实现会抛出并丢掉
所有已获取数据，所以这一点比原实现更健壮）；`Future.wait` 保序，
`_dedupeGrades` 看到的顺序与串行版一致，去重结果不变。

**回归保护**：`test/direct_gateway_grades_concurrency_test.dart` 3 个用例。
其中并发性用例让 8 个请求互相阻塞直到全部到达才放行——串行实现会在此死锁，
能通过即证明真正并发。另有保序用例（故意让后到的学期先返回）与
单学期失败不影响其余 7 个的用例。

### 9.7 通知点击无跳转（P1，已修复）

**证据类型：代码检索 + 路由测试。**

`notification_service.dart:79` 调用 `_plugin.initialize(settings)`，
未传 `onDidReceiveNotificationResponse`；全平台包检索
`onDidReceiveNotificationResponse` / `getNotificationAppLaunchDetails` /
`payload` 仅命中 `background_task.dart:723` 一处（且无对应处理）。

后果：课前提醒、考试提醒、电费/校园卡余额预警，点击后只是冷启到第一个 tab。
而桌面小组件的深链路由**已经完整建好**——`WidgetNavigationService`
（MethodChannel + `consumePendingTarget`）+ `main.dart:172-186, 277-300`
处理 4 个 target，包含付款码的滚动信号。管道现成，通知没接上。

一个不能带你去看当天课表的课前提醒，是产品语义上的断裂。

**修复**：定义与小组件深链**同一套** target 词汇（`schedule` / `electricity`
/ `campus_card` / `app_update`），让两个来源汇入同一个 `_handleWidgetTarget`
路由；给课前提醒、电费预警、校园卡预警、后台余额通知补 `payload`；
`initialize` 接入 tap 回调；补冷启动两条路径——
`getNotificationAppLaunchDetails`（从终止态被通知启动时回调不触发，必须显式读）
与 pending target 重放（点击发生在 UI 注册 handler 之前）。

`app_update` 单独路由到"我的"tab 而非落到 `default` 分支（会去课表），
因为更新卡片在那里，且没有可跳入的流程。

**回归保护**：`packages/platform/test/services/notification_tap_routing_test.dart`
5 个用例——正常投递、冷启动重放、重放仅一次、四个 target 全覆盖、
以及一条锁住 target 字符串与小组件词汇一致的用例（任何一侧改名都会让
路由静默落到 default 分支，不报错也不挂测试）。

### 9.8 交互与无障碍（P1/P2，部分已修复）

**已修复：**

**切 tab 整页重建。** `responsive_scaffold.dart:51` 用
`AnimatedSwitcher` + `KeyedSubtree(key: ValueKey(currentIndex))`，每次切换
给 tab 换新 key，Flutter 销毁旧 element 树并从零重建：服务/我的的滚动位置
每次丢失，`CampusCardPage` 的 `initState` 自动刷新每次返回都重发请求，
crossfade 期间两个页面同时布局。全 app 零 `IndexedStack` / `AutomaticKeepAlive`
/ `PageStorageKey`。
改为 `IndexedStack` + `Visibility(maintainState: true)`。
回归保护：`test/widgets/responsive_scaffold_keepalive_test.dart` 3 个用例——
每个 tab 只构造一次（往返 3 次后 initState 计数仍为 1）、滚动位置往返后
完整保留、离屏 tab 不绘制。

**AppListTile 的 120ms 导航延迟。** `app_list_tile.dart:79-89` 把 `onTap`
包在 `Future.delayed(AppMotion.press)` 里等按压动画播完。该组件是"服务"tab
所有行的基础，等于给 app 内每一次服务跳转加了 120ms；且 `if (mounted)` 守卫
会在这 120ms 内组件被卸载时**静默丢弃这次点击**。改为用 `Listener` 的
指针事件驱动按压动画，回调即时触发。
回归保护：`test/widgets/app_list_tile_tap_test.dart` 3 个用例——同帧触发
（故意不 `pumpAndSettle`，延迟实现会在此失败）、点击导致自身卸载时不丢 tap、
按压动画仍然工作。

**无障碍定点修复。** 全 app 零 `Semantics` / 零 `semanticLabel`
（24 个无障碍相关命中全是 `tooltip:`）。已修复其中影响最大的几处：
`schedule_page.dart:280` 刷新按钮缺 tooltip（隔壁 286 行有）；
`schedule_week_navigator.dart:57,114` 周导航按钮为 36×36，低于 Material /
WCAG 2.5.5 的 48dp 下限 25%，且它们是 app 内最常点的控件——已提至 48dp
并补 tooltip；周次长按选择器是纯 `GestureDetector`、对 TalkBack 完全不可发现，
已补 `Semantics(button: true, label: '选择周次', hint: '长按…')`。

**登录表单 autofill。** `login_page.dart:313` 缺 `autofillHints`，
Android 密码管理器无法识别此表单、无法填充。已补
`AutofillHints.username` / `AutofillHints.password` 与 `textInputAction`；
密码可见性切换按钮的 tooltip 随状态变化（在密码已显示时播报"显示密码"
会告诉屏幕阅读器用户与实际相反的信息）。

**未修复（见 9.9）：** 设计系统组件层未被消费、下拉刷新覆盖不足、
长列表 eager 构建、其余无障碍缺口、深色模式。

### 9.9 未修复项与决策依据

以下为第二轮识别但本轮未实施的项，附不实施的理由。这些是有意留下的，
不是遗漏。

**1. 深色模式（9.3）。** 需先把 120 处硬编码 `Colors.white` 收敛进 token 层，
再补全 `darkTheme`，属于跨全部 18 个文件的系统性改动。在没有真机逐屏
视觉验收的条件下批量替换颜色，回归风险高于收益。建议作为独立一轮工作，
配合真机逐页截图对比推进。

**2. 设计系统组件层未被消费（实测数据）。**

| 组件 | 在 pages 中使用次数 |
|------|-------------------|
| `AppCard` | **0（死代码）** |
| `AppBadge` | **0（死代码）** |
| `AppButton` | 1（仅登录页） |
| `AppListTile` | 1 |
| `AppEntrance` | 30 ✓ |

`AppType.` 使用 **3 次**，对上 **262 处内联 `TextStyle(`**——五级光学字阶
实际未生效，字号各处现拍（实测跨越 9/9.5/10/…/48 二十余档）。卡片有 4 种
并存实现（`Card(`、`brandedCardDecoration()`、裸 `Container+BoxDecoration`、
`Material(color: white)`）。

不在本轮做的理由：这是纯外观重构，涉及全部页面，且没有任何自动化手段
能验证"改完之后长得一样"。它需要的是逐页视觉回归，而非单元测试。
token 层本身质量很高，问题只在消费侧——属于可以持续渐进偿还的债务，
不适合一次性批量替换。

**3. 下拉刷新覆盖（2/9 页面）。** 仅 `academic_status_page.dart:91` 与
跑步页有 `RefreshIndicator`。课表页的网格含横向滚动，`RefreshIndicator`
会与横向拖拽产生手势竞争，需要先设计手势仲裁方案（或只在网格外层区域生效），
不宜直接套上。其余 5 个纯纵向列表页可以低风险补齐，留作下一轮。

**4. 长列表 eager 构建。** `grades_section.dart` 用 spread 展开全部成绩行
（`...result.grades.map(...)`），默认"查全部"下高年级学生会一次性构建
60+ 行；`study_progress_page.dart` 的培养计划同理（约 150-200 行）。
隔壁 `exams_section.dart:76` 已正确使用 `ListView.builder`。
第一轮报告已判定此项需要"低端机帧级证据"才实施（见第五部分第 2 条），
本轮未获得该证据，遵循原判定不实施。

**5. `zh_CH` locale 拼写。** `main.dart:114`
`supportedLocales: const [Locale('zh', 'CH')]`——`CH` 是瑞士，中国大陆应为 `CN`。
因 `GlobalMaterialLocalizations` 会按语言回退，中文字符串仍能加载，
所以当前无可见故障；但任何区域敏感的格式化和未来的 `zh_Hant` 处理都建立在
错误标签上。属于低风险单行修复，但改动会影响 locale 解析路径，
建议与深色模式那一轮一起做真机验证。

**6. `_handleWidgetTarget` 的 `popUntil` 破坏用户状态。**
`main.dart:279` 无条件 `popUntil((route) => route.isFirst)`——用户正在填
"新增自定义课程"或处于 WebView 登录流程中时，一次小组件/通知点击会静默
销毁这些状态。本轮已为 `app_update` 目标做了分流，但通用的
"跳转前确认是否丢弃未保存内容"需要引入路由级的脏状态追踪，
超出本轮范围。

**7. 硬编码版本号（工作区未提交改动引入）。**
`profile_background.dart` 新增的 `_handleVersionTap` 硬编码
`'当前版本：v1.0.0+4 (Release)'`，与同文件 `:24` 真实加载的 `_versionLabel`
并存，且它挂在一个与 `ListTile.onTap`（`_checkUpdate`）重叠的
`GestureDetector` 上——两个重叠点击区、不同行为、无视觉区分。
该改动属未提交工作区内容，建议直接改用 `_versionLabel` 或移除该手势。

**8. 跑步功能整体未接线。** `CampusRunningPage` / `CampusRunningBridgePage`
/ `CampusRunningTrackerPage`（合计约 1,978 行）在自身目录外零引用，
`campusRunningBetaProvider` 也无人读取，用户无法进入。属未提交的在建功能。
接线前的正确性前提（账号隔离）已在 9.5 修复。

关于该功能已排除的两项风险：
- **手写 MD5 是正确的**（`sports_schema_auth_service.dart:8`）。
  用 6 个 RFC 1321 标准向量 + 8 个分组边界长度（55/56/57/63/64/65/119/120）
  与系统 `md5sum` 逐一对照，全部一致。Dart 的 32/64 位溢出未造成问题。
- **GPS 过滤比预期完整**：精度 >35m 丢点（`gps_running_service.dart:237`）
  + 瞬移 >15m/s 丢点（`:255`），Haversine 实现标准。

### 9.10 第二轮修复汇总

| 项 | 级别 | 状态 | 回归测试 |
|----|------|------|---------|
| 9.1 加载态/错误态死代码 | P0 | 已修复 | 8 用例 |
| 9.2 GlassAppBar 毛玻璃失效 | P0 | 已修复 | 4 用例 |
| 9.5 跑步记录账号隔离 | P0 | 已修复 | 4 用例 |
| 9.4 包体 82.8→24.8MB | P1 | 已修复 | 4 用例 |
| 9.6 成绩 9 串行请求并发化 | P1 | 已修复 | 3 + 1 用例 |
| 9.7 通知点击深链路由 | P1 | 已修复 | 5 用例 |
| 9.8 切 tab 整页重建 | P2 | 已修复 | 3 用例 |
| 9.8 AppListTile 120ms 延迟 | P2 | 已修复 | 3 用例 |
| 9.8 无障碍定点 + autofill | P1 | 部分修复 | 随页面测试 |
| 9.3 深色模式 | P0 | **未修复** | — |
| 9.9-2 设计系统消费 | P1 | **未修复** | — |
| 9.9-3 下拉刷新覆盖 | P1 | **未修复** | — |
| 9.9-4 长列表 eager 构建 | P2 | **未修复**（无帧级证据） | — |
| 9.9-5~8 其余 | P2 | **未修复** | — |

测试总数 291 → **326**（+35），四包 analyze 零问题，`dart format` 零改动，
四个覆盖率门禁全部通过（core 72.4% / data 44.5% / platform 66.0% / app 30.2%，
其中 data +3.6pp、app +2.0pp）。

详细实施记录见下一节。

---
---

## 实施状态（2026-08-22 第二轮）

### 已实施（含测试）

| 报告条目 | 落实方式 | 测试 |
|---------|---------|------|
| 9.1 加载态/错误态死代码 | `when()` 按 缓存数据 > loading > error 重排分支；移除 6 个调用点自相矛盾的 `skipError: true`；4 处裸 `e.toString()` 接入 `formatCampusError`；`exams_section` 补重试按钮；课表页保留可操作的会话过期专用文案 | `cached_resource_when_branch_test.dart`（8 用例） |
| 9.2 GlassAppBar 失效 | 改订阅 `ScrollNotificationObserver`（Scaffold 在 appBar/body 之上提供）；过滤非垂直轴与 `depth != 0` | `glass_app_bar_scroll_test.dart`（4 用例） |
| 9.5 跑步账号隔离 | 键按用户名作用域化；两个 notifier 接入 `Ref` 监听 `credentialsProvider`；换账号先清内存再加载；异步读完成校验账号未变；登出用独立 `__signed_out` 作用域 | `running_account_isolation_test.dart`（4 用例） |
| 9.4 包体 | `--split-per-abi`（−46.6MB）；PDF 字体固定 Regular 字重 + GB2312 子集化 7,589 码位（−15.3MB）；logo 2048→288（−2.2MB）；发布流程写入 `docs/release-build.md`；gradle 留注释说明不要重复尝试 `splits{abi{}}` | `schedule_pdf_font_test.dart`（4 用例） |
| 9.6 成绩并发化 | 汇总页先行 + 8 学期页 `Future.wait`；每学期独立 try/catch 降级；保序不改变去重结果。前置修复 `forceRelogin` 并发风暴（in-flight 复用） | `direct_gateway_grades_concurrency_test.dart`（3 用例）+ `direct_gateway_relogin_storm_test.dart`（1 用例） |
| 9.7 通知点击路由 | 与小组件深链共用 target 词汇；四类通知补 `payload`；`initialize` 接 tap 回调；补 `getNotificationAppLaunchDetails` 冷启动路径与 pending 重放；`app_update` 分流到"我的" | `notification_tap_routing_test.dart`（5 用例） |
| 9.8 切 tab 重建 | `AnimatedSwitcher + KeyedSubtree` 改 `IndexedStack + Visibility(maintainState: true)` | `responsive_scaffold_keepalive_test.dart`（3 用例） |
| 9.8 AppListTile 延迟 | 移除 `Future.delayed` 门控，按压动画改由 `Listener` 指针事件驱动 | `app_list_tile_tap_test.dart`（3 用例） |
| 9.8 无障碍 + autofill | 课表刷新按钮补 tooltip；周导航 36→48dp 并补 tooltip；周次长按补 `Semantics`；登录补 `autofillHints` + `textInputAction`；密码可见性 tooltip 随状态变化 | 随既有页面测试回归 |

### 验证结果

| 项 | 结果 |
|----|------|
| `packages/core` | 49 通过 |
| `packages/data` | 25 通过 + 5 跳过（live 测试需真实凭据） |
| `packages/platform` | 81 通过 |
| `apps/campus_app` | 171 通过 |
| **合计** | **326 通过，5 跳过，0 失败**（第一轮 291） |
| 四包 `dart analyze` / `flutter analyze` | 全部 `No issues found!` |
| `dart format --set-exit-if-changed .` | 164 文件，0 改动 |
| 覆盖率门禁 | core 72.4%/60% · data 44.5%/35% · platform 66.0%/50% · app 30.2%/25%，全过 |
| arm64-v8a Release APK | 24.8MB（原 82.8MB） |

### 测试有效性验证

新增测试中有两条做了"反向验证"——先确认测试对目标缺陷敏感，再修复：

1. **9.1**：修复前探针输出 `冷启动 -> DATA("")` / `首次失败 -> DATA("")`，
   修复后为 `LOADING` / `ERROR`。
2. **9.6**：临时移除 `forceRelogin` 互斥后，测试报出 9 次 CAS 登录
   （`Expected: <= 2, Actual: <9>`）；恢复后为 2 次。

第 9.2 的 4 个用例中，"body 滚动后模糊启用"这一条在修复前实测失败
（sigma 恒为 0.0），修复后通过。

这三处反向验证的目的是避免重复第一轮的问题模式——测试存在且通过，
但并未真正约束目标行为。

### 未实施项

见 9.9，共 8 项，每项附不实施的具体理由。其中深色模式（9.3）与设计系统
消费（9.9-2）是剩余最大的两块，均因"需要真机逐页视觉验收、无法用自动化
测试保证正确性"而留作独立一轮工作。长列表 eager 构建（9.9-4）遵循第一轮
报告"需低端机帧级证据"的原判定，本轮未获得该证据故不实施。

### 被替换资源的保管

`--split-per-abi` 与子集化替换掉的原始资源移入仓库根目录 `.trash/`
（未使用 `rm`）：

- `NotoSansSC-VF.ttf`（17.8MB 完整可变字体）— 需要扩大子集字符集时从此取源
- `campus_app_mark_2048_original.png`（2048×2048 原图）— 需要更高分辨率图标时取源
- `_pinned.ttf`（子集化中间产物）
- `_verify_when_test.dart`（9.1 的一次性探针）